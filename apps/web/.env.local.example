# Monitoring Server URL (BetterAuth backend)
NEXT_PUBLIC_MONITORING_API_URL=http://localhost:3333

# Frontend App URL (for OAuth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:4000

# Auth proxy behavior (set to "true" to allow fail-open in production)
AUTH_FAIL_OPEN=
